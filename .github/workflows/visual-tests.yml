name: Visual Tests

on:
  push:
    branches: [main]
    paths:
      - 'src/ui/**'
      - 'src/main.rs'
      - 'tapes/**'
      - '.github/workflows/visual-tests.yml'
  pull_request:
    branches: [main]
    paths:
      - 'src/ui/**'
      - 'src/main.rs'
      - 'tapes/**'
      - '.github/workflows/visual-tests.yml'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Generate screenshots from tape files
  generate-screenshots:
    name: Generate Screenshots
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libchafa-dev libglib2.0-dev libasound2-dev xvfb

      - name: Install VHS
        run: |
          curl -sL -o vhs.deb https://github.com/charmbracelet/vhs/releases/download/v0.11.0/vhs_0.11.0_amd64.deb
          sudo dpkg -i vhs.deb || sudo apt-get install -f -y
          vhs --version

      - name: Install ttyd
        run: |
          curl -sL -o ttyd https://github.com/tsl0922/ttyd/releases/download/1.7.7/ttyd.x86_64
          sudo install ttyd /usr/local/bin/ttyd
          ttyd --version

      - name: Build for VHS
        run: cargo build --release

      - name: Create screenshots directory
        run: mkdir -p screenshots/current

      - name: Run VHS tapes
        env:
          DISPLAY: :99
        run: |
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 2
          for tape in tapes/*.tape; do
            echo "Running: $tape"
            timeout 120 vhs "$tape" || echo "Tape completed or timed out: $tape"
          done

      - name: Collect screenshots
        run: |
          # Move generated screenshots to current directory
          find screenshots -name "*.png" -exec mv {} screenshots/current/ \; 2>/dev/null || true
          ls -la screenshots/current/ || echo "No screenshots generated"

      - name: Upload screenshots
        uses: actions/upload-artifact@v7
        with:
          name: screenshots-current
          path: screenshots/current/
          if-no-files-found: warn

      - name: Store reference screenshots
        if: github.ref == 'refs/heads/main'
        run: |
          mkdir -p screenshots/reference
          cp screenshots/current/*.png screenshots/reference/ 2>/dev/null || true

      - name: Upload reference screenshots
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          name: screenshots-reference
          path: screenshots/reference/
          if-no-files-found: warn

  # Generate demo GIF for README and landing page
  generate-demo:
    name: Generate Demo GIF
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libchafa-dev libglib2.0-dev libasound2-dev xvfb

      - name: Install VHS
        run: |
          curl -sL -o vhs.deb https://github.com/charmbracelet/vhs/releases/download/v0.11.0/vhs_0.11.0_amd64.deb
          sudo dpkg -i vhs.deb || sudo apt-get install -f -y
          vhs --version

      - name: Install ttyd
        run: |
          curl -sL -o ttyd https://github.com/tsl0922/ttyd/releases/download/1.7.7/ttyd.x86_64
          sudo install ttyd /usr/local/bin/ttyd
          ttyd --version

      - name: Build for VHS
        run: cargo build --release

      - name: Generate demo GIF
        env:
          DISPLAY: :99
        run: |
          Xvfb :99 -screen 0 1920x1080x24 &
          sleep 2
          timeout 120 vhs tapes/readme_demo.tape || true
          # The GIF is a best-effort docs nicety: VHS is flaky under Xvfb and a
          # missing GIF must not turn the Visual Tests badge red. Say clearly
          # which happened.
          if [ -f assets/demo.gif ]; then
            ls -la assets/demo.gif
          else
            echo "::warning::VHS did not produce assets/demo.gif; skipping the demo GIF update"
          fi

      - name: Upload demo GIF
        uses: actions/upload-artifact@v7
        with:
          name: demo-gif
          path: assets/demo.gif
          if-no-files-found: warn

  # Commit demo GIF back to repo on main
  update-readme-assets:
    name: Update README Assets
    needs: generate-demo
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # The artifact only exists when VHS actually produced a GIF. A missing
      # artifact is an expected outcome, not a failure — without this the job
      # errored on every run and pinned the README badge to "failing".
      - name: Download demo GIF
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          name: demo-gif
          path: assets/

      - name: Check for changes
        id: check
        run: |
          if [ -f assets/demo.gif ]; then
            git add assets/demo.gif
            if ! git diff --cached --quiet; then
              echo "changed=true" >> $GITHUB_OUTPUT
            else
              echo "changed=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "::notice::No demo GIF artifact to commit"
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit demo GIF
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: update demo GIF from VHS [skip ci]"
          git push
